# exaroton API Token
# Get your API token from: https://exaroton.com/account
EXAROTON_API_TOKEN=your_api_token_here

# Server Configuration
PORT=3000
NODE_ENV=development

# Exaroton Server ID - The specific Minecraft server this bot controls (REQUIRED for bot)
DEFAULT_SERVER_ID=your_exaroton_server_id_here

# Discord Bot Configuration
# Get from: https://discord.com/developers/applications
DISCORD_BOT_TOKEN=your_discord_bot_token_here
DISCORD_CLIENT_ID=your_discord_application_id_here

# Role configuration
# Users must have this role to run most bot commands
DISCORD_REQUIRED_ROLE=Bot
# Role required to run /start and /tempbanvote (defaults to 'Approved' if unset)
DISCORD_START_ROLE=Approved

# Tempban command format (optional)
# Use {player} for player name and {reason} for ban reason
# Commands WITHOUT / prefix - Exaroton API adds it automatically
# Example from docs: server.executeCommand("say Hello world!") NOT "/say Hello world!"
# Examples:
#   tempban {player} 30m {reason}    (default - requires EssentialsX, LiteBans, or AdvancedBan)
#   ban {player} {reason}             (vanilla Minecraft permanent ban)
#   kick {player} {reason}            (alternative: just kick)
TEMPBAN_COMMAND_FORMAT=tempban {player} 30m {reason}

# Run Mode - determines which application to start
# RUN_MODE=api    (runs web dashboard)
# RUN_MODE=bot    (runs Discord bot)
RUN_MODE=api
